Wann soll die Maschine spätestens fertig sein?<br>
<% (0..session[:time].length-1).each do |digit| %>
<% if digit == 1 %>
:
<% end  %>
<% if digit == session[:tindex] then %>
<b>
<% end %>
<%=if session[:time][digit].to_s.length == 1 then "0#{session[:time][digit].to_s}" else session[:time][digit] end =%>
<% if digit == session[:tindex] then %>
</b>
<% end %>
<% end %>
